<svg width="1200" height="300" viewBox="0 0 1200 300" xmlns="http://www.w3.org/2000/svg">
  <!-- Hintergrund -->
  <rect width="100%" height="100%" fill="#000000"/>

  <!-- Äußerer Rahmen -->
  <rect x="20" y="20" width="1160" height="260"
        fill="none" stroke="#00ff00" stroke-width="4" />

  <!-- SHIFT6 -->
  <text x="50%" y="100"
        fill="#00ff00"
        font-family="Courier New, monospace"
        font-size="56"
        text-anchor="middle"
        letter-spacing="10">
    SHIFT6
  </text>

  <!-- Titel -->
  <text x="50%" y="160"
        fill="#00ff00"
        font-family="Courier New, monospace"
        font-size="44"
        text-anchor="middle"
        letter-spacing="6">
    TOTAL FREE RPGLE FORMATTER
  </text>

  <!-- Untertitel (nach oben versetzt + kleiner) -->
  <text x="50%" y="215"
        fill="#00ff00"
        font-family="Courier New, monospace"
        font-size="24"
        text-anchor="middle"
        letter-spacing="4">
    KEEP **FREE IN COLUMN 1 • SHIFT ALL OTHER LINES AUTOMATICALLY
  </text>
</svg>
